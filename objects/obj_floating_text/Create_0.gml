// obj_floating_text - Create Event

// 初始化變數 (這些值通常會在創建實例後由創建者修改)
display_text = "0";     // 預設顯示文字
text_color = c_red;  // 預設文字顏色

// 動畫參數
float_speed = 0.8;     // 向上浮動速度 (每幀向上移動的像素)
fade_speed = 0.02;     // 淡出速度 (每幀 alpha 減少量)
// lifespan = 60;         // 可以移除或保留 lifespan 作為備用銷毀機制

image_alpha = 1.0;     // 初始透明度

// 設置深度，確保在其他物件之上
depth = -1000; // 或一個比單位和特效更小的值

// --------------------------------------------------
// 字體和對齊設置建議移到 Draw 事件中
// 因為 draw_set_* 函數會影響全局繪圖狀態
// 在 Create 中設置可能會影響其他物件的繪製
// --------------------------------------------------